# Remotwerk — 프로젝트 지침

## 협업 방식 (반드시 지킬 것)

1. **CLI 안내 금지.** 사용자에게 터미널 명령어를 주고 직접 실행하라고 안내하지 않는다.
   `npm install`, `npm start`, `npm run dist`, `git pull` 같은 것을 시키지 않는다.
2. **내가 할 수 있는 일은 내가 한다.** 코드 작성·수정, git 커밋/푸시, 빌드, 테스트,
   CI 실행과 결과 확인은 전부 직접 수행하고 **결과만 보고**한다.
   - 리눅스에서 Windows 설치파일을 만들려면 `wine` 이 필요해 로컬 빌드가 막힌다.
     → **GitHub Actions 의 windows 러너로 빌드**하고, 워크플로 실행·로그 확인까지 직접 한다.
3. **사용자 계정으로만 가능한 일만 요청한다.** Firebase 콘솔 설정, 결제/요금제, 코드 서명
   인증서 구매, 저장소 공개 전환 등. 이때는 **화면 클릭 경로**로 안내한다:
   **메뉴 이름 → 탭 이름 → 버튼 이름** 순서. 명령어가 아니라 클릭할 대상을 적는다.
4. 검증하지 못한 것은 **검증하지 못했다고 명시**한다. 추정을 확인된 사실처럼 말하지 않는다.

## 제품 개요

연인 두 사람이 **서로의 바탕화면에 캐릭터를 상주**시키고 **몸짓으로 신호**를 주고받는
데스크톱 앱. 메신저가 아니라 **답장을 요구하지 않는 낮은 대역폭의 현존(presence) 아바타**다.

**설계 원칙 4가지 (위반 금지)**
1. **비침습성** — 업무를 방해하지 않는다. 캐릭터 밖은 클릭이 통과한다.
2. **무압박** — 읽음/응답 표시를 만들지 않는다.
3. **곁눈질 가독성** — 신호 하나 = 또렷한 동작 하나.
4. **상태 비감시** — 캐릭터의 자율 생활은 상대의 접속 여부를 반영하지 않는다.

## 기술 구조

- **Electron** (메인: 창·트레이·단축키 / 렌더러: 오버레이·리모컨·히스토리·설정·리깅·뷰어)
- **Firebase** 프로젝트 `remotwerk-aa0d2` — 익명 인증 + Realtime Database(싱가포르
  `asia-southeast1`) + Storage
- 렌더러 공유 모듈은 **클래식 스크립트 + `window.RW` 네임스페이스** (번들러 없음).
  `file://` 에서 ES 모듈 import 가 막히기 때문이다. Firebase SDK 만 CDN 동적 import.

**보안 모델(중요):** 접근 권한은 "코드를 아는가"가 아니라 **"내 uid 가 그 방의 좌석(a/b)에
앉아 있는가"** 로 판단한다. 초대 코드는 1회용 + 24시간 만료이며 접근 열쇠가 아니다.
RTDB 규칙에는 `numChildren()` 이 **없으므로** 정원 제한은 좌석 방식으로 구현했다.

**애니메이션:** 캐릭터가 아니라 **골격**에 붙는 키프레임 시퀀스. 능동 8 + 자율 2 + 보조 1.
5조각 커스텀 캐릭터는 `animSource` 로 어깨/골반 트랙만 읽어 근사 재생한다.

## 작업 규칙

- 개발 브랜치: `claude/remotwerk-desktop-app-lcys33`. **`main` 에도 함께 푸시**한다
  (사용자가 GitHub Desktop 으로 `main` 을 쓰기 때문).
- 커밋 메시지는 한국어. 무엇을·왜 바꿨는지 적는다.
- 렌더러 로직을 바꾸면 Node 하네스로 회귀 테스트를 돌린다(`npm test`).
- **리그·애니메이션·캐릭터 아트를 건드리면 정지 포즈만 보고 판단하지 않는다.**
  `tools/anim-check/` 로 모든 동작의 모든 프레임을 그려서 자동 검사 + 시트를 눈으로 확인한다.
  (v0.6.0 에서 뉴트럴만 보고 "정상"이라 보고했다가, 움직이면 몸이 찢어지는 걸 놓쳤다.)
- 규칙 파일(`database.rules.json`, `storage.rules`)을 바꾸면 사용자가 콘솔에 붙여넣어야
  하므로, 변경 시 **전체 내용을 채팅에 다시 제시**한다.
