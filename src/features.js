'use strict';

/* 기능 플래그 — 제품 방향에 따라 켜고 끄는 스위치.
 *
 * **메인 프로세스와 렌더러가 이 파일 하나만 본다.** 값을 두 군데 적어 두면 반드시
 * 어긋나므로, preload 가 이 객체를 그대로 렌더러에 전달한다(window.rwHost.features).
 *
 * 화면에서 숨기는 것만으로는 끈 게 아니다. CSS 로 가려도 창 열기 요청은 그대로 가고,
 * 그 요청을 메인이 받아 창을 띄우면 기능은 살아 있는 것이다. 그래서 **메인 쪽 진입점
 * (트레이 메뉴·IPC 핸들러·createRigger)도 같은 플래그로 막는다.**
 */

module.exports = {
  // 사용자가 이미지를 올려 캐릭터를 만들고 편집하는 기능(리깅 도구).
  //
  // 제품 방향이 "우리가 제작·검수한 캐릭터를 고르는 서비스" 로 바뀌면서 기본 OFF 다.
  // **구현은 그대로 두고 진입점만 막는다** — 나중에 확장 기능으로 다시 켠다.
  //
  // 끄더라도 이미 만들어 둔 개인 캐릭터(config.customCharacters)는 건드리지 않는다.
  // 목록에 그대로 보이고, 고를 수 있고, 상대에게 전달되고, 재시작해도 남는다.
  // 없어지는 것은 "새로 만들기" 와 "편집" 버튼뿐이다.
  characterUpload: false
};
